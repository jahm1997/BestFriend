const { Router } = require("express");
const router = Router();

// const agregarFavoritos = require("../controllers/addFavorite.js");
// const getFavorites = require("../controllers/getFavorites.js");
// const deleteFavorite = require("../controllers/deleteFavorite.js");
// //o podrias usar const { Router } = require("express") y despues const router = Router()

// router.post("/", agregarFavoritos); //función para meter en favoritos.js
// router.get("/", getFavorites); //función para traerlos fav
// router.delete("/:id", deleteFavorite); //functión que filtre todos menos el id

module.exports = router;
